# vue-v1.0.0 — 🎯 收口与对比文档定稿

| 项 | 值 |
| --- | --- |
| 需求 | VAC-2 ~ VAC-11 |
| 关键 V-ADR | 全部 |
| 预计耗时 | 3 ~ 4 小时 |
| 上一个 tag | `vue-v0.14.0` |
| React 对应 | [`fe-v1.0.0`](../../frontend/tags/17-fe-v1.0.0-收口.md) |

> 🎯 **本 tag 的主要产出不是代码，是
> [04-React与Vue3做法对比.md](../04-React与Vue3做法对比.md) 的定稿。**
>
> 如果做完这个阶段只有一个能跑的 Vue 前端，而对比文档还是骨架，
> **这个阶段就白做了**（`01-PRD.md` 第 1.1 节）。

---

## 一、交付物清单

| 操作 | 文件 | 职责 |
| --- | --- | --- |
| 新建 | `frontend-vue/CLAUDE.md` | Vue 前端的工程约定 |
| 新建 | `scripts/smoke_all_vue_tags.sh` | 遍历 `vue-*` tag 冒烟 |
| 修改 | `deploy/nginx.conf` | 加 `/vue/` 入口 |
| 修改 | `README.md` | **三套前端**的入口说明 |
| 修改 | CI 配置 | 两个前端的常量校验 + 两套测试 |
| 🎯 修改 | `docs/vue-frontend/04-React与Vue3做法对比.md` | **定稿：填实测结论** |
| 修改 | `docs/vue-frontend/0[1-3]-*.md` | 与实现不符处同步更正 |

---

## 二、验收（逐条跑）

```bash
# 🔒 最重要的两条
git diff main HEAD -- apps/rbac/services.py           # 期望：空（第四次）
git diff fe-v1.0.0 HEAD -- apps/ config/              # 期望：只有两个管理命令

# 后端
pytest -q                                             # 417 全绿

# Vue 前端
cd frontend-vue
npm run typecheck && npm run build
npm run test -- --coverage                            # 权限模块 ≥ 90%
bash ../scripts/e2e-vue.sh                            # 越权矩阵全绿

# 🔒 React 版没被弄坏
cd ../frontend
npm run test                                          # 98 全绿
bash ../scripts/e2e.sh                                # 92 全绿

# 权限常量：两个前端都与后端一致，且彼此相同
cd .. && python manage.py export_perm_constants --check
python manage.py export_perm_constants --out frontend-vue/src/constants/permissions.ts --check
diff frontend/src/constants/permissions.ts frontend-vue/src/constants/permissions.ts
```

| VAC | 项 | 判定 |
| --- | --- | --- |
| VAC-2 | 全部 P0 需求 | 逐条对照 PRD |
| VAC-3 | 15 个 tag 能独立运行 | `smoke_all_vue_tags.sh` |
| VAC-4 | 🔒 内核未被修改 | 上面第一条命令 |
| VAC-5 | 🔒 后端几乎未改 | 上面第二条命令 |
| VAC-6 | 7 个用户故事 | E2E |
| VAC-7 | 权限模块覆盖率 | vitest |
| VAC-8 | 🎯 E2E 断言逐字相同 | `matrixParity` + 人工 diff |
| VAC-9 | 后端测试全绿 | pytest |
| VAC-10 | 🔒 React 版仍然全绿 | 两个套件 |
| VAC-11 | 🎯 **对比文档 15 个主题都有两边真实代码** | 人工评审 |

---

## 三、⚠️ 收口时最该检查的三件事

### 1. `VAC-4` / `VAC-5` 守住了吗

```bash
git diff fe-v1.0.0 HEAD --stat -- apps/ config/
```

**改动应该只有两个文件**：

```
apps/rbac/management/commands/export_perm_constants.py
apps/rbac/management/commands/export_enforced_perms.py
```

**如果 `apps/rbac/services.py` 出现在列表里，本阶段的架构承诺就没兑现。**
那不是小瑕疵——它意味着「内核不认识表现层」这条约束，
**在遇到第四种表现层时才破了**，而前三次都没破。

⚠️ 如果 `apps/rbac/api/` 或 `apps/tickets/` 出现在列表里，也要停下来解释：
React 阶段付出的适配成本，本来应该被完整继承。

### 2. 🎯 对比文档真的填了吗（`VAC-11`）

逐项检查 [04 文档](../04-React与Vue3做法对比.md)：

- [ ] 15 个主题每个都有**两边的真实代码**（不是描述）
- [ ] 每个主题都有「各自会踩的坑」一栏
- [ ] 「附：还没验证的三件事」全部填了实测结论：
  - [ ] `watch` 是否还有「`await refetch()` 后 store 未更新」的窗口（`vue-v0.11.0`）
  - [ ] `autoInsertSpaceInButton` 是不是以「照抄导致无效属性」的形式踩到（`vue-v0.2.0`）
  - [ ] E2E 除三处例外是否真的一字未改（`vue-v0.14.0`）
- [ ] 16.2 的决策统计与实际相符（4 继承 / 2 微调 / 9 重新决策）
- [ ] 16.4 的失误方向对照表，每一行都是**你自己踩过或验证过的**

⚠️ **预判错了的地方要如实改，不要为了让文档好看而把结论往预判上凑。**
`01-PRD.md` 第 1.3 节列了 6 条预判——**错了的那几条最有价值**。

### 3. 每个 tag 规格书的「与 React 版的差异」一节

`VNFR-12` 要求 15 份规格书**每一份**都有这一节，且无差异时**显式写「无差异」**。

> 「无差异」不是省略，是**结论**。
> 写「无差异」和不写，信息量完全不同：
> 前者说明「这一层与框架无关」，后者说明「作者没想过这个问题」。

---

## 四、阶段回顾

### 4.1 重新评估几条 V-ADR

📌 **下面留白，由你实现完之后填写。** 与 React 版 `fe-v1.0.0` 的做法一致：
**你自己实现过一遍再来对照，分歧的地方比一致的地方有价值。**

| V-ADR | 事后评价 |
| --- | --- |
| **002** 最小差异原则 | |
| **003** Pinia + 延迟调用 | |
| **004** `addRoute` + 手动清理 | |
| **005** 导航期守卫 | |
| **007** 不用 `v-perm` 指令 | |
| **009** `queryKey` 一律 `computed` | |
| **012** E2E 逐字复用 | |
| **013** 不用文件式路由 | |

参考问题：
- 这条决策**执行过程中动摇过吗**？在哪个 tag？
- 代价那一栏预估准了吗？
- 如果重来，会不会选另一个方案？

### 4.2 四个交付形态的对比

到这里，同一个权限内核已经服务了**四种表现层**：

| | Django 模板 | DRF + JWT | React SPA | **Vue SPA** |
| --- | --- | --- | --- | --- |
| tag | `v1.0.0` | `v1.2.0` | `fe-v1.0.0` | `vue-v1.0.0` |
| 后端适配层新增 | ~200 行 | **117 行** | ~700 行 | **≈20 行** |
| 内核改动 | — | **0** | **0** | **0** |
| 权限变更生效 | 刷新即生效 | 每次请求 | 一次请求 + 感知 | 一次请求 + 感知 |
| 鉴权层数 | 3 | 1（API） | **4** | **4** |
| 安全边界数 | **1** | **1** | **1** | **1** |

📌 **实测数据**（收口时填写）：

| 项 | 数值 |
| --- | --- |
| 后端测试 | **417** 全绿（**未增加**——这本身是结论） |
| Vue 前端单测 | 待填 |
| Vue E2E | 待填 |
| React 版单测 / E2E | **98 / 92 仍然全绿** |
| 🔒 `git diff main HEAD -- apps/rbac/services.py` | **0 行** |
| 🔒 `git diff fe-v1.0.0 HEAD -- apps/ config/` | **2 个文件** |

**「后端测试数量未增加」是本阶段独有的一行。**
React 阶段从 365 涨到 417（+52），因为它挖出了 4 个真实问题并各自补了测试。
Vue 阶段**一个新的后端问题都没挖出来**——

> 📌 这不是「Vue 版做得不够仔细」，而是
> **接第二个前端的排查价值已经被 React 阶段兑现完了。**
>
> 那 4 个问题（派单下拉框泄露全部用户、数据范围无审计、
> `seed_demo --flush` 崩溃、树接口分页丢分支）**都不是「React 怎么写」的问题**，
> 而是「接第二个前端才暴露」的问题。接第三个前端**不会再暴露一遍**。
>
> **接第 N 个前端的边际价值是递减的**——所以本阶段的主要产出
> 才必须是**对比文档**，而不是「又一个能跑的前端」。

### 4.3 回答 `fe-v1.0.0` 留下的那个问题

> **把前端换成 Vue 再走一遍。哪些 tag 的规格书一个字都不用改？**

| 答案层级 | 内容 |
| --- | --- |
| **最强**：整个 tag 消失 | `fe-v0.2.0`（后端 Session + CSRF）、`fe-v0.5.0`（后端路由字段）**2 个** |
| **很强**：断言逐字不变 | E2E 越权矩阵（`fe-v0.16.0`）——**这是验收项** |
| **强**：文件逐字复用 | `csrf.ts`、`api/*.ts`、MSW handlers、fixtures、`no-client-side-filtering` |
| **中**：逻辑相同实现不同 | `menuAdapter`、`buildRoutes`、`<Can>`、`formErrors`、`useTableQuery` |
| **弱**：需要重新设计 | 状态实例化、动态路由注册、守卫位置——**只有这三处** |

### 4.4 📌 本阶段的三条结论

**结论 1：权限架构的层次划分与框架无关。**

四层体验 + 一层安全，在两个框架里结构完全相同。
换框架只改变「每一层长什么样」，不改变「有几层、哪一层是边界」。

**结论 2：两边的失误方向系统性地相反。**

- React 更容易「更新太多」，Vue 更容易「更新太少」
- **在权限系统里 Vue 的方向更危险**（该消失的按钮没消失是功能 bug）
- 但 React 更容易「失败得很静」（死锁、顺序相关的测试），Vue 更容易「失败得很响」

**任何一句「X 更适合权限系统」都会漏掉上面某一半。**

**结论 3：写起来更短的那一边，不一定是更难写错的那一边。**

**代码量和正确性是两个独立的维度。**
大部分框架对比只比第一个，而只有第二个会在半年后找上门。

---

## 五、`frontend-vue/CLAUDE.md` 要写什么

必须包含（否则后来者会「优化」掉关键决策）：

| 节 | 内容 |
| --- | --- |
| 0 | **前端权限是体验，不是安全**（与 React 版同一句话） |
| 1 | 技术栈与版本，⚠️ 标注 antdv 4 ≠ antd 6 |
| 2 | 状态归属三条线 + **`storeToRefs` 的硬规则** |
| 3 | 目录与依赖方向 + **`useAuthStore()` 不许在模块顶层** |
| 4 | 权限硬规则 + **为什么不用 `v-perm` 指令**（指向 V-ADR-007） |
| 5 | 错误处理表（🔴 `401 \|\| 403` 那一行） |
| 6 | **`queryKey` 一律 `computed`**（V-ADR-009） |
| 7 | **`clearDynamicRoutes` 不能忘**（V-ADR-004） |
| 8 | **不用文件式路由**（V-ADR-013，与 Vue 生态主流方向相反，必须写明） |
| 9 | 测试分层 + `setActivePinia` |
| 10 | 每个 tag 的 DoD（含「React 版没被弄坏」） |

---

## 六、自测清单

| # | 用例 | 期望 |
| --- | --- | --- |
| 1 | 第二节的每一条命令 | 全过 |
| 2 | `smoke_all_vue_tags.sh` | 15 个 tag 都能起 |
| 3 | 三个前端同时可用 | :5173 / :5174 / `/django/` |
| 4 | 对比文档 15 个主题 | 都有两边真实代码 |
| 5 | 「还没验证的三件事」 | 都填了实测结论 |
| 6 | 15 份规格书的「与 React 版的差异」 | 都写了（含「无差异」） |
| 7 | `01-PRD.md` 的 6 条预判 | 逐条标注对/错，错的说明为什么 |

---

## 七、延伸练习

1. **接第四个前端（Svelte / Solid）时，本阶段的哪些结论还成立？**
   先写下预测，再花两小时只做「登录 + profile + 一个 `<Can>`」验证一下。
   **预测错的地方，说明你对「与框架无关」的判断还不够准。**

2. **实现 [V-ADR-007](../02-设计文档.md) 里被否决的 `v-perm` 指令的「修正版」**：
   用 `v-if` 的时机（`beforeMount` + 返回 VNode）而不是删 DOM。
   **能修好缺陷 1 和 2 吗？缺陷 3（类型丢失）呢？**

3. **挑一条「已知简化」实现它**（[02-设计文档](../02-设计文档.md) 第 5 节的 11 条）。
   推荐 **#9**：把 `clearDynamicRoutes()` 挂到 store 的 `$reset` 订阅上，
   让「登出」只有一个入口。这会带出 Pinia 的插件机制。

4. **合并回主干**：试着把 `feat/vue-frontend` 合进 `main`。
   冲突面比 `feat/react-frontend` 大还是小？**为什么？**

5. **反过来做一次**：假设你**先**做 Vue 版，再做 React 版。
   哪些 V-ADR 会变成「继承」，哪些 F-ADR 会变成「重新决策」？
   **这个对称性说明了什么？**
